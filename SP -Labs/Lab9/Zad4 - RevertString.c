#include <stdio.h>
#include <stdlib.h>

revertString(char *a, char *b, char *comparator)
{
    //comparator = pomal/pogolem     if ne sa tija Invalid comparator
}
int main()
{
    printf("Hello world!\n");
    return 0;
}
